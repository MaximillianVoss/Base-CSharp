<UserControl x:Class="CustomControlsWPF.LabeledComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomControlsWPF"
             mc:Ignorable="d" MinHeight="75" MaxHeight="75" MinWidth="100">
    <Grid x:Name="gMain" Background="White" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="lblTitle" Content="Labelg" VerticalAlignment="Center" Height="26"/>
        <ComboBox x:Name="cbItems" Margin="5,0,5,0" VerticalAlignment="Center" SelectionChanged="cbItems_SelectionChanged" Grid.Row="1" Height="20"/>
        <Label x:Name="lblError" Content="Labelg" Foreground="#FFDE2020" Grid.Row="2" Height="26"/>

    </Grid>
</UserControl>
